const { oldNames, newNames } = require('./names')
const words = require('../wordplay/words')

// There is at least one baby namne from the top 40 baby names for 2020 that,
// when spelled backwards, is a valid scrabble word. Find and print all such names.
// Solve this using an array to hold the Scrabble words.

function solution() {

  // ...

}